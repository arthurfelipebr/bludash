# Exemplo de variáveis de ambiente
# Copie este arquivo para .env e edite os valores conforme sua necessidade
PORT=3001
PGHOST=localhost
PGPORT=5432
PGUSER=seu_usuario
PGPASSWORD=sua_senha
PGDATABASE=bludb
JWT_SECRET=sua-chave-secreta
API_KEY=sua-chave-gemini
# Opcional: URL do backend para proxy durante desenvolvimento
VITE_API_PROXY=http://localhost:3001
